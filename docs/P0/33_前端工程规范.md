# MOY 前端工程规范

---

## 文档元信息

| 属性 | 内容 |
|------|------|
| 文档名称 | MOY 前端工程规范 |
| 文档编号 | MOY_FE_ENGINEERING_001 |
| 版本号 | v1.0 |
| 状态 | 已确认 |
| 作者 | MOY 文档架构组 |
| 日期 | 2026-04-05 |
| 目标读者 | 前端开发工程师 |
| 输入来源 | [开发规范文档](./12_开发规范文档.md)、[HLD](./09_HLD_系统高层设计.md) |

---

## 一、文档目的

本文档定义 MOY 系统的前端工程规范，确保：

1. 项目结构统一、可维护
2. 代码风格一致
3. 开发流程标准化
4. 工程配置规范化
5. 支持团队协作开发

---

## 二、目录结构规范

### 2.1 项目根目录

```
moy-web/
├── .github/                    # GitHub 配置
│   └── workflows/              # CI/CD 工作流
├── .husky/                     # Git Hooks
├── public/                     # 静态资源
│   ├── favicon.ico
│   └── logo.svg
├── src/                        # 源代码
│   ├── components/             # 公共组件
│   ├── pages/                  # 页面组件
│   ├── hooks/                  # 自定义 Hooks
│   ├── services/               # API 服务
│   ├── stores/                 # 状态管理
│   ├── utils/                  # 工具函数
│   ├── types/                  # 类型定义
│   ├── constants/              # 常量定义
│   ├── styles/                 # 全局样式
│   ├── assets/                 # 静态资源
│   ├── layouts/                # 布局组件
│   ├── routers/                # 路由配置
│   ├── contexts/               # React Context
│   ├── App.tsx                 # 应用入口
│   ├── main.tsx                # 渲染入口
│   └── vite-env.d.ts           # Vite 类型声明
├── .env                        # 环境变量
├── .env.development            # 开发环境变量
├── .env.production             # 生产环境变量
├── .eslintrc.cjs               # ESLint 配置
├── .prettierrc                 # Prettier 配置
├── .gitignore                  # Git 忽略配置
├── index.html                  # HTML 入口
├── package.json                # 项目配置
├── pnpm-lock.yaml              # 依赖锁定
├── postcss.config.js           # PostCSS 配置
├── tailwind.config.js          # Tailwind 配置
├── tsconfig.json               # TypeScript 配置
├── tsconfig.node.json          # Node TypeScript 配置
└── vite.config.ts              # Vite 配置
```

### 2.2 src 目录详解

```
src/
├── components/                 # 公共组件
│   ├── base/                   # 基础组件（二次封装）
│   │   ├── Button/
│   │   │   ├── index.tsx
│   │   │   ├── Button.test.tsx
│   │   │   └── types.ts
│   │   └── ...
│   ├── business/               # 业务组件
│   │   ├── CustomerCard/
│   │   │   ├── index.tsx
│   │   │   ├── CustomerCard.test.tsx
│   │   │   └── types.ts
│   │   └── ...
│   └── layout/                 # 布局组件
│       ├── MainLayout/
│       ├── Sidebar/
│       └── Header/
│
├── pages/                      # 页面组件
│   ├── customer/               # 客户模块
│   │   ├── CustomerListPage/
│   │   │   ├── index.tsx
│   │   │   ├── components/     # 页面私有组件
│   │   │   │   └── CustomerFilter/
│   │   │   ├── hooks/          # 页面私有 Hooks
│   │   │   │   └── useCustomerList.ts
│   │   │   └── types.ts
│   │   ├── CustomerDetailPage/
│   │   └── CustomerCreatePage/
│   ├── lead/
│   ├── opportunity/
│   └── ...
│
├── hooks/                      # 全局 Hooks
│   ├── useAuth.ts
│   ├── usePermission.ts
│   ├── useWebSocket.ts
│   └── ...
│
├── services/                   # API 服务
│   ├── api/                    # API 定义
│   │   ├── customer.ts
│   │   ├── lead.ts
│   │   └── ...
│   ├── request/                # 请求封装
│   │   ├── index.ts
│   │   ├── interceptors.ts
│   │   └── error.ts
│   └── websocket/              # WebSocket 封装
│       ├── index.ts
│       └── types.ts
│
├── stores/                     # 状态管理
│   ├── useAuthStore.ts
│   ├── useUserStore.ts
│   ├── useNotificationStore.ts
│   └── ...
│
├── utils/                      # 工具函数
│   ├── format.ts               # 格式化
│   ├── validate.ts             # 校验
│   ├── storage.ts              # 存储
│   ├── download.ts             # 下载
│   └── ...
│
├── types/                      # 全局类型
│   ├── api.ts                  # API 通用类型
│   ├── customer.ts
│   ├── lead.ts
│   └── ...
│
├── constants/                  # 常量定义
│   ├── status.ts               # 状态常量
│   ├── routes.ts               # 路由常量
│   ├── storage.ts              # 存储键常量
│   └── ...
│
├── styles/                     # 全局样式
│   ├── global.css              # 全局样式
│   ├── variables.css           # CSS 变量
│   └── animations.css          # 动画样式
│
├── routers/                    # 路由配置
│   ├── index.tsx               # 路由入口
│   ├── routes.tsx              # 路由定义
│   └── guards.tsx              # 路由守卫
│
├── layouts/                    # 布局组件
│   ├── MainLayout/
│   └── BlankLayout/
│
└── contexts/                   # React Context
    ├── AuthContext.tsx
    └── ThemeContext.tsx
```

---

## 三、路由规范

### 3.1 路由定义规范

```tsx
// routers/routes.tsx
import { lazy } from 'react';
import type { RouteObject } from 'react-router-dom';

const CustomerListPage = lazy(() => import('@/pages/customer/CustomerListPage'));
const CustomerDetailPage = lazy(() => import('@/pages/customer/CustomerDetailPage'));

export const routes: RouteObject[] = [
  {
    path: '/',
    element: <MainLayout />,
    children: [
      {
        index: true,
        element: <DashboardPage />,
      },
      {
        path: 'customers',
        element: <CustomerListPage />,
        handle: {
          title: '客户列表',
          permission: 'customer:read',
        },
      },
      {
        path: 'customers/:id',
        element: <CustomerDetailPage />,
        handle: {
          title: '客户详情',
          permission: 'customer:read',
        },
      },
    ],
  },
];
```

### 3.2 路由守卫规范

```tsx
// routers/guards.tsx
import { Navigate, useLocation } from 'react-router-dom';
import { useAuthStore } from '@/stores/useAuthStore';

interface AuthGuardProps {
  children: React.ReactNode;
  permission?: string;
}

export function AuthGuard({ children, permission }: AuthGuardProps) {
  const { isAuthenticated, hasPermission } = useAuthStore();
  const location = useLocation();

  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  if (permission && !hasPermission(permission)) {
    return <Navigate to="/403" replace />;
  }

  return <>{children}</>;
}
```

### 3.3 路由常量定义

```typescript
// constants/routes.ts
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  CUSTOMERS: {
    LIST: '/customers',
    DETAIL: (id: string | number) => `/customers/${id}`,
    CREATE: '/customers/create',
    EDIT: (id: string | number) => `/customers/${id}/edit`,
  },
  LEADS: {
    LIST: '/leads',
    DETAIL: (id: string | number) => `/leads/${id}`,
    CREATE: '/leads/create',
  },
  // ...
} as const;
```

---

## 四、API Client 规范

### 4.1 Axios 实例配置

```typescript
// services/request/index.ts
import axios from 'axios';
import type { AxiosInstance, AxiosRequestConfig } from 'axios';
import { setupInterceptors } from './interceptors';
import { handleApiError } from './error';

const BASE_URL = import.meta.env.VITE_API_BASE_URL;

const request: AxiosInstance = axios.create({
  baseURL: BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

setupInterceptors(request);

export async function get<T>(url: string, config?: AxiosRequestConfig): Promise<T> {
  const response = await request.get<T>(url, config);
  return response.data;
}

export async function post<T>(url: string, data?: unknown, config?: AxiosRequestConfig): Promise<T> {
  const response = await request.post<T>(url, data, config);
  return response.data;
}

export async function put<T>(url: string, data?: unknown, config?: AxiosRequestConfig): Promise<T> {
  const response = await request.put<T>(url, data, config);
  return response.data;
}

export async function del<T>(url: string, config?: AxiosRequestConfig): Promise<T> {
  const response = await request.delete<T>(url, config);
  return response.data;
}
```

### 4.2 拦截器配置

```typescript
// services/request/interceptors.ts
import type { AxiosInstance, AxiosError } from 'axios';
import { useAuthStore } from '@/stores/useAuthStore';
import { message } from 'antd';

export function setupInterceptors(instance: AxiosInstance) {
  instance.interceptors.request.use(
    (config) => {
      const { token } = useAuthStore.getState();
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      const orgId = useAuthStore.getState().currentOrgId;
      if (orgId) {
        config.headers['X-Org-Id'] = orgId;
      }
      return config;
    },
    (error) => Promise.reject(error)
  );

  instance.interceptors.response.use(
    (response) => {
      const { data } = response;
      if (data.code !== 0) {
        message.error(data.message || '请求失败');
        return Promise.reject(new Error(data.message));
      }
      return response;
    },
    (error: AxiosError) => {
      if (error.response?.status === 401) {
        useAuthStore.getState().logout();
        window.location.href = '/login';
      } else if (error.response?.status === 403) {
        message.error('无权限访问');
      } else if (error.response?.status === 500) {
        message.error('服务器错误，请稍后重试');
      }
      return Promise.reject(error);
    }
  );
}
```

### 4.3 API 服务定义

```typescript
// services/api/customer.ts
import { get, post, put, del } from '../request';
import type { Customer, CustomerListParams, CustomerListResponse } from '@/types/customer';

export const customerApi = {
  list: (params: CustomerListParams) => 
    get<CustomerListResponse>('/api/v1/customers', { params }),
  
  get: (id: number) => 
    get<Customer>(`/api/v1/customers/${id}`),
  
  create: (data: Partial<Customer>) => 
    post<Customer>('/api/v1/customers', data),
  
  update: (id: number, data: Partial<Customer>) => 
    put<Customer>(`/api/v1/customers/${id}`, data),
  
  delete: (id: number) => 
    del<void>(`/api/v1/customers/${id}`),
  
  export: (params: CustomerListParams) => 
    get<Blob>('/api/v1/customers/export', { 
      params, 
      responseType: 'blob' 
    }),
};
```

---

## 五、类型定义规范

### 5.1 类型文件组织

```typescript
// types/api.ts - API 通用类型
export interface ApiResponse<T = unknown> {
  code: number;
  message: string;
  data: T;
  requestId: string;
  timestamp: string;
}

export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}

export interface PaginatedParams {
  page?: number;
  pageSize?: number;
  sortBy?: string;
  sortOrder?: 'asc' | 'desc';
}
```

```typescript
// types/customer.ts - 客户类型
export interface Customer {
  id: number;
  orgId: number;
  name: string;
  phone?: string;
  email?: string;
  company?: string;
  level: CustomerLevel;
  status: CustomerStatus;
  source: CustomerSource;
  ownerId: number;
  owner?: User;
  tags: string[];
  createdAt: string;
  updatedAt: string;
}

export type CustomerLevel = 'A' | 'B' | 'C' | 'D';
export type CustomerStatus = 'potential' | 'new_customer' | 'active' | 'silent' | 'lost';
export type CustomerSource = 'website' | 'referral' | 'event' | 'cold_call' | 'other';

export interface CustomerListParams extends PaginatedParams {
  keyword?: string;
  status?: CustomerStatus;
  level?: CustomerLevel;
  ownerId?: number;
  dateFrom?: string;
  dateTo?: string;
}

export type CustomerListResponse = PaginatedResponse<Customer>;
```

### 5.2 类型命名规范

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 实体类型 | PascalCase | Customer, Lead |
| 枚举类型 | PascalCase | CustomerStatus |
| 参数类型 | 实体名 + Params | CustomerListParams |
| 响应类型 | 实体名 + Response | CustomerListResponse |
| Props 类型 | 组件名 + Props | CustomerCardProps |
| 联合类型 | PascalCase | Status |

---

## 六、状态管理规范

### 6.1 Zustand Store 规范

```typescript
// stores/useAuthStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { userApi } from '@/services/api/user';

interface User {
  id: number;
  name: string;
  email: string;
  roles: string[];
  permissions: string[];
}

interface AuthState {
  token: string | null;
  user: User | null;
  currentOrgId: number | null;
  isAuthenticated: boolean;
  
  // Actions
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  setUser: (user: User) => void;
  hasPermission: (permission: string) => boolean;
  hasAnyPermission: (permissions: string[]) => boolean;
  hasAllPermissions: (permissions: string[]) => boolean;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      token: null,
      user: null,
      currentOrgId: null,
      isAuthenticated: false,

      login: async (email, password) => {
        const response = await userApi.login({ email, password });
        set({
          token: response.token,
          user: response.user,
          currentOrgId: response.user.orgId,
          isAuthenticated: true,
        });
      },

      logout: () => {
        set({
          token: null,
          user: null,
          currentOrgId: null,
          isAuthenticated: false,
        });
      },

      setUser: (user) => set({ user }),

      hasPermission: (permission) => {
        const { user } = get();
        if (!user) return false;
        return user.permissions.includes('*') || user.permissions.includes(permission);
      },

      hasAnyPermission: (permissions) => {
        const { hasPermission } = get();
        return permissions.some((p) => hasPermission(p));
      },

      hasAllPermissions: (permissions) => {
        const { hasPermission } = get();
        return permissions.every((p) => hasPermission(p));
      },
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        token: state.token,
        user: state.user,
        currentOrgId: state.currentOrgId,
      }),
    }
  )
);
```

### 6.2 React Query 规范

```typescript
// hooks/useCustomerList.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { customerApi } from '@/services/api/customer';
import type { CustomerListParams } from '@/types/customer';

export function useCustomerList(params: CustomerListParams) {
  return useQuery({
    queryKey: ['customers', 'list', params],
    queryFn: () => customerApi.list(params),
    staleTime: 5 * 60 * 1000, // 5分钟
  });
}

export function useCustomer(id: number) {
  return useQuery({
    queryKey: ['customers', 'detail', id],
    queryFn: () => customerApi.get(id),
    enabled: id > 0,
  });
}

export function useCreateCustomer() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: customerApi.create,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['customers', 'list'] });
    },
  });
}

export function useUpdateCustomer() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({ id, data }: { id: number; data: Partial<Customer> }) =>
      customerApi.update(id, data),
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ queryKey: ['customers', 'list'] });
      queryClient.invalidateQueries({ queryKey: ['customers', 'detail', variables.id] });
    },
  });
}
```

---

## 七、表单校验规范

### 7.1 React Hook Form + Zod 规范

```typescript
// hooks/useCustomerForm.ts
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';

const customerSchema = z.object({
  name: z.string().min(1, '请输入客户名称').max(100, '客户名称最多100个字符'),
  phone: z.string().regex(/^1[3-9]\d{9}$/, '请输入有效的手机号').optional().or(z.literal('')),
  email: z.string().email('请输入有效的邮箱').optional().or(z.literal('')),
  company: z.string().max(200, '公司名称最多200个字符').optional(),
  level: z.enum(['A', 'B', 'C', 'D']),
  source: z.enum(['website', 'referral', 'event', 'cold_call', 'other']),
  ownerId: z.number().min(1, '请选择负责人'),
  tags: z.array(z.string()).max(10, '最多添加10个标签'),
});

type CustomerFormData = z.infer<typeof customerSchema>;

export function useCustomerForm(defaultValues?: Partial<CustomerFormData>) {
  return useForm<CustomerFormData>({
    resolver: zodResolver(customerSchema),
    defaultValues: {
      name: '',
      phone: '',
      email: '',
      company: '',
      level: 'C',
      source: 'other',
      ownerId: 0,
      tags: [],
      ...defaultValues,
    },
  });
}
```

### 7.2 通用校验规则

```typescript
// utils/validate.ts
export const validationRules = {
  required: (message = '此字段为必填项') => ({
    required: true,
    message,
  }),
  
  phone: (message = '请输入有效的手机号') => ({
    pattern: /^1[3-9]\d{9}$/,
    message,
  }),
  
  email: (message = '请输入有效的邮箱') => ({
    type: 'email' as const,
    message,
  }),
  
  maxLength: (max: number) => ({
    max,
    message: `最多输入${max}个字符`,
  }),
  
  minLength: (min: number) => ({
    min,
    message: `最少输入${min}个字符`,
  }),
  
  url: (message = '请输入有效的URL') => ({
    type: 'url' as const,
    message,
  }),
};
```

---

## 八、权限控制实现

### 8.1 权限 Hook

```typescript
// hooks/usePermission.ts
import { useAuthStore } from '@/stores/useAuthStore';

export function usePermission() {
  const { hasPermission, hasAnyPermission, hasAllPermissions } = useAuthStore();

  return {
    hasPermission,
    hasAnyPermission,
    hasAllPermissions,
    
    canCreate: (resource: string) => hasPermission(`${resource}:create`),
    canRead: (resource: string) => hasPermission(`${resource}:read`),
    canUpdate: (resource: string) => hasPermission(`${resource}:update`),
    canDelete: (resource: string) => hasPermission(`${resource}:delete`),
    canExport: (resource: string) => hasPermission(`${resource}:export`),
  };
}
```

### 8.2 权限组件

```tsx
// components/business/PermissionGuard/index.tsx
import { usePermission } from '@/hooks/usePermission';

interface PermissionGuardProps {
  permission?: string | string[];
  mode?: 'any' | 'all';
  fallback?: React.ReactNode;
  children: React.ReactNode;
}

export function PermissionGuard({
  permission,
  mode = 'any',
  fallback = null,
  children,
}: PermissionGuardProps) {
  const { hasPermission, hasAnyPermission, hasAllPermissions } = usePermission();

  if (!permission) return <>{children}</>;

  const permissions = Array.isArray(permission) ? permission : [permission];
  
  const hasAccess = mode === 'all'
    ? hasAllPermissions(permissions)
    : hasAnyPermission(permissions);

  return hasAccess ? <>{children}</> : <>{fallback}</>;
}
```

---

## 九、WebSocket 接入规范

### 9.1 WebSocket 客户端封装

```typescript
// services/websocket/index.ts
import { useAuthStore } from '@/stores/useAuthStore';

type MessageHandler = (data: unknown) => void;

class WebSocketClient {
  private ws: WebSocket | null = null;
  private reconnectAttempts = 0;
  private maxReconnectAttempts = 10;
  private handlers: Map<string, Set<MessageHandler>> = new Map();
  private heartbeatInterval: NodeJS.Timeout | null = null;

  connect() {
    const { token } = useAuthStore.getState();
    const wsUrl = `${import.meta.env.VITE_WS_URL}?token=${token}`;

    this.ws = new WebSocket(wsUrl);

    this.ws.onopen = () => {
      console.log('WebSocket connected');
      this.reconnectAttempts = 0;
      this.startHeartbeat();
    };

    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data);
      this.handleMessage(message);
    };

    this.ws.onclose = () => {
      console.log('WebSocket closed');
      this.stopHeartbeat();
      this.scheduleReconnect();
    };

    this.ws.onerror = (error) => {
      console.error('WebSocket error:', error);
    };
  }

  private startHeartbeat() {
    this.heartbeatInterval = setInterval(() => {
      this.send('ping', { timestamp: Date.now() });
    }, 30000);
  }

  private stopHeartbeat() {
    if (this.heartbeatInterval) {
      clearInterval(this.heartbeatInterval);
      this.heartbeatInterval = null;
    }
  }

  private scheduleReconnect() {
    if (this.reconnectAttempts < this.maxReconnectAttempts) {
      const delay = Math.min(1000 * Math.pow(2, this.reconnectAttempts), 30000);
      setTimeout(() => this.connect(), delay);
      this.reconnectAttempts++;
    }
  }

  send(type: string, payload: unknown) {
    if (this.ws?.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify({ type, payload, timestamp: Date.now() }));
    }
  }

  subscribe(type: string, handler: MessageHandler) {
    if (!this.handlers.has(type)) {
      this.handlers.set(type, new Set());
    }
    this.handlers.get(type)!.add(handler);
  }

  unsubscribe(type: string, handler: MessageHandler) {
    this.handlers.get(type)?.delete(handler);
  }

  private handleMessage(message: { type: string; payload: unknown }) {
    const handlers = this.handlers.get(message.type);
    handlers?.forEach((handler) => handler(message.payload));
  }

  disconnect() {
    this.stopHeartbeat();
    this.ws?.close(1000, 'Client disconnect');
    this.ws = null;
  }
}

export const wsClient = new WebSocketClient();
```

### 9.2 WebSocket Hook

```typescript
// hooks/useWebSocket.ts
import { useEffect } from 'react';
import { wsClient } from '@/services/websocket';

export function useWebSocket(type: string, handler: (data: unknown) => void) {
  useEffect(() => {
    wsClient.subscribe(type, handler);
    return () => wsClient.unsubscribe(type, handler);
  }, [type, handler]);
}

export function useNotification() {
  const [notifications, setNotifications] = useState<Notification[]>([]);

  useWebSocket('notification', (data) => {
    setNotifications((prev) => [data as Notification, ...prev]);
  });

  return { notifications };
}
```

---

## 十、埋点接入规范

### 10.1 埋点工具封装

```typescript
// utils/analytics.ts
interface TrackEvent {
  category: string;
  action: string;
  label?: string;
  value?: number;
}

class Analytics {
  private enabled: boolean;

  constructor() {
    this.enabled = import.meta.env.PROD;
  }

  pageView(pageName: string) {
    if (!this.enabled) return;
    // 发送页面浏览事件
    this.track({ category: 'page', action: 'view', label: pageName });
  }

  event(category: string, action: string, label?: string, value?: number) {
    if (!this.enabled) return;
    this.track({ category, action, label, value });
  }

  private track(event: TrackEvent) {
    // 实际埋点上报逻辑
    console.log('[Analytics]', event);
  }
}

export const analytics = new Analytics();
```

### 10.2 埋点 Hook

```typescript
// hooks/useAnalytics.ts
import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';
import { analytics } from '@/utils/analytics';

export function usePageView(pageName: string) {
  useEffect(() => {
    analytics.pageView(pageName);
  }, [pageName]);
}

export function useTrackEvent() {
  return {
    track: (category: string, action: string, label?: string, value?: number) => {
      analytics.event(category, action, label, value);
    },
  };
}
```

---

## 十一、Mock 约定

### 11.1 Mock 文件组织

```
mock/
├── index.ts              # Mock 入口
├── customer.ts           # 客户模块 Mock
├── lead.ts               # 线索模块 Mock
├── opportunity.ts        # 商机模块 Mock
└── ...
```

### 11.2 Mock 数据规范

```typescript
// mock/customer.ts
import type { Customer, CustomerListResponse } from '@/types/customer';

export const mockCustomers: Customer[] = [
  {
    id: 1,
    orgId: 1,
    name: '示例客户A',
    phone: '13800138001',
    email: 'example_a@test.com',
    company: '示例公司A',
    level: 'A',
    status: 'active',
    source: 'website',
    ownerId: 1,
    tags: ['VIP', '重点客户'],
    createdAt: '2026-04-01T10:00:00Z',
    updatedAt: '2026-04-05T10:00:00Z',
  },
  // ...
];

export const mockCustomerListResponse: CustomerListResponse = {
  items: mockCustomers,
  total: 100,
  page: 1,
  pageSize: 20,
};
```

---

## 十二、错误处理规范

### 12.1 错误边界

```tsx
// components/ErrorBoundary.tsx
import { Component } from 'react';
import { Result, Button } from 'antd';

interface Props {
  children: React.ReactNode;
  fallback?: React.ReactNode;
}

interface State {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends Component<Props, State> {
  state: State = { hasError: false };

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || (
        <Result
          status="500"
          title="页面出错了"
          subTitle="抱歉，页面遇到了问题"
          extra={
            <Button type="primary" onClick={() => window.location.reload()}>
              刷新页面
            </Button>
          }
        />
      );
    }

    return this.props.children;
  }
}
```

### 12.2 全局错误处理

```typescript
// utils/error.ts
import { message } from 'antd';

export function handleApiError(error: unknown, defaultMessage = '操作失败') {
  if (error instanceof Error) {
    message.error(error.message || defaultMessage);
  } else {
    message.error(defaultMessage);
  }
}

export function handleFormError(error: unknown, form: any) {
  if (error instanceof Error) {
    // 处理表单级错误
    form.setFields([{ name: 'root', errors: [error.message] }]);
  }
}
```

---

## 十三、国际化预留

### 13.1 国际化配置预留

```typescript
// i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

i18n.use(initReactI18next).init({
  lng: 'zh-CN',
  fallbackLng: 'zh-CN',
  resources: {
    'zh-CN': {
      translation: {
        // 翻译资源
      },
    },
  },
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

### 13.2 国际化使用预留

```tsx
// 使用示例（首期不启用，但代码中预留）
import { useTranslation } from 'react-i18next';

function Component() {
  const { t } = useTranslation();
  
  return <h1>{t('customer.title')}</h1>;
}
```

---

## 十四、主题/品牌配置预留

### 14.1 主题配置

```typescript
// theme/index.ts
import type { ThemeConfig } from 'antd';

export const defaultTheme: ThemeConfig = {
  token: {
    colorPrimary: '#1890ff',
    colorSuccess: '#52c41a',
    colorWarning: '#faad14',
    colorError: '#ff4d4f',
    borderRadius: 6,
  },
};

export const brandThemes: Record<string, ThemeConfig> = {
  default: defaultTheme,
  // 预留其他品牌主题
};
```

### 14.2 主题切换预留

```tsx
// contexts/ThemeContext.tsx
import { createContext, useContext, useState } from 'react';
import { ConfigProvider, theme } from 'antd';
import { defaultTheme } from '@/theme';

interface ThemeContextValue {
  isDark: boolean;
  toggleTheme: () => void;
}

const ThemeContext = createContext<ThemeContextValue | null>(null);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [isDark, setIsDark] = useState(false);

  const toggleTheme = () => setIsDark((prev) => !prev);

  return (
    <ThemeContext.Provider value={{ isDark, toggleTheme }}>
      <ConfigProvider
        theme={{
          ...defaultTheme,
          algorithm: isDark ? theme.darkAlgorithm : undefined,
        }}
      >
        {children}
      </ConfigProvider>
    </ThemeContext.Provider>
  );
}

export const useTheme = () => useContext(ThemeContext);
```

---

## 十五、版本与变更记录

| 版本 | 日期 | 作者 | 变更摘要 | 状态 |
|------|------|------|----------|------|
| v1.0 | 2026-04-05 | MOY 文档架构组 | 初稿 | 已确认 |

---

## 十六、依赖文档

| 文档 | 版本 | 用途 |
|------|------|------|
| [12_开发规范文档.md](./12_开发规范文档.md) | v1.0 | 开发规范 |
| [09_HLD_系统高层设计.md](./09_HLD_系统高层设计.md) | v1.0 | 架构设计 |
| [25_前端UI组件库规范.md](./25_前端UI组件库规范.md) | v1.0 | UI组件规范 |
| [29_WebSocket协议规范.md](./29_WebSocket协议规范.md) | v1.0 | WebSocket协议 |

---

## 十七、待确认事项

1. 是否需要支持微前端架构？
2. 是否需要支持 PWA 离线访问？
3. 是否需要支持多标签页状态同步？
