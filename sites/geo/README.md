# MOY GEO — AI 搜索增长服务站

- 域名：`geo.moy.com`
- 本地端口：`5176`
- 定位：AI 搜索增长与品牌可见度服务营销站

## 启动

```bash
cd sites/geo
npm install
npm run dev
```

## 构建部署

```bash
npm run build     # 输出到 dist/
npm run preview   # 本地预览构建产物
```

部署时将 `dist/` 指向 `geo.moy.com` 的静态文件服务。
